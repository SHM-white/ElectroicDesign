[pytest]
testpaths =
    drone/test
    tools
addopts =
    --ignore=drone/test/test_all.py
markers =
    field_data: requires external image fixtures recorded in drone/test/fixtures/field-images.json
    hardware: requires physical target hardware
    flight: requires an actual flight environment
