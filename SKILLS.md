# 技能使用指南

## 可用技能

### 1. delegate-tasks (任务委派)
**触发条件**：当需要将具体任务委派给子代理时使用
**使用场景**：
- 将代码实现任务委派给通用代理
- 将搜索任务委派给探索代理
- 将测试任务委派给测试代理

**使用方法**：
```
使用delegate-tasks技能来委派具体任务给子代理
```

### 2. subagent-orchestrator (子代理协调)
**触发条件**：当需要同时协调多个子代理时使用
**使用场景**：
- 并行处理多个独立任务
- 流水线处理复杂任务
- 专家模式分配任务

**使用方法**：
```
使用subagent-orchestrator技能来协调多个子代理并行处理任务
```

### 3. wsl-powershell-shell (WSL PowerShell)
**触发条件**：当需要在Windows PowerShell中运行Linux命令时使用
**使用场景**：
- 在WSL环境中执行Linux命令
- 运行.sh脚本和Bash管道
- 使用WSL工具

**使用方法**：
```
使用wsl-powershell-shell技能来在PowerShell中运行Linux命令
```

## 技能选择指南

### 任务委派 vs 子代理协调
- **任务委派**：适合简单的单任务委派
- **子代理协调**：适合复杂的多任务并行处理

### 选择标准
1. **任务复杂度**：复杂任务使用子代理协调
2. **并行度**：需要并行处理时使用子代理协调
3. **依赖关系**：有依赖关系时使用任务委派
4. **资源需求**：资源密集型任务使用子代理协调

## 最佳实践

### 技能使用原则
1. **明确目标**：清楚知道要完成什么任务
2. **选择合适技能**：根据任务特性选择最合适的技能
3. **提供上下文**：为技能提供足够的背景信息
4. **评估结果**：检查技能执行结果是否符合预期

### 技能组合使用
1. **探索-实现**：先用探索技能分析，再用实现技能执行
2. **并行处理**：同时使用多个技能处理不同任务
3. **渐进式**：逐步使用技能完成复杂任务

## 配置要求

### 技能配置
确保在 `opencode.json` 中正确配置了技能路径：
```json
{
  "skills": {
    "paths": [".opencode/skills"]
  }
}
```

### 代理配置
确保为不同技能配置了合适的代理：
- 主代理使用 `xiaomi/mimo-v2.5-pro`
- 子代理使用 `deepseek/deepseek-v4-flash`
- 为不同代理设置适当的权限

## 故障排除

### 技能未触发
1. 检查技能描述是否匹配当前任务
2. 确认技能文件路径正确
3. 验证技能配置是否生效

### 技能执行失败
1. 检查代理权限设置
2. 验证模型配置是否正确
3. 查看错误日志定位问题

### 性能问题
1. 调整并行任务数量
2. 优化任务描述和上下文
3. 检查系统资源使用情况

## 更新和维护

### 技能更新
1. 定期检查技能文件是否需要更新
2. 根据使用反馈优化技能描述
3. 添加新的技能以满足需求

### 配置优化
1. 根据使用情况调整代理配置
2. 优化技能触发条件
3. 改进技能执行流程