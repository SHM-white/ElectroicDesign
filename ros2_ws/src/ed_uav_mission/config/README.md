# Simulation mission

`missions/simulation_patrol.yaml` is a short synthetic patrol for a one-to-three
minute simulator smoke run. It must only be launched through the simulator
mission launch with `simulation_only:=true`; it is not a hardware mission.
